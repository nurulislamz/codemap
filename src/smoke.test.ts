import { describe, expect, it } from "vitest";

describe("project scaffold", () => {
  it("runs the Vitest baseline", () => {
    expect("leetcode-backend-helper").toBe("leetcode-backend-helper");
  });
});
